# Landing page illustration assets

Export these from the Figma design and drop them in this folder using **exactly
these filenames**. The rebuilt landing page references them by these paths, so once
they're here they appear automatically (no code change needed).

SVG preferred for illustrations (crisp + scalable); JPG/PNG fine for photos.

| Filename | Where it's used | Source in design |
|---|---|---|
| `hero.svg` | Hero section | Person sitting with laundry basket + hanging shirt |
| `step-schedule.svg` | How it works → "Schedule a pickup" | Person leaning, clouds/sparkles |
| `step-wash.svg` | How it works → "We wash, you work" | Person + washing machine + SOAP box |
| `step-delivered.svg` | How it works → "Delivered fresh" | Washing machines + basket + hanging shirt |
| `step-rewards.svg` | How it works → "Earn rewards" (kept step) | (reuse/any rewards-themed illustration) |
| `cta.svg` | Final CTA → "Give your teams their Saturday back" | Person jumping with clean clothes on a line |
| `testimonial-1.jpg` | Social proof card 1 | Left portrait |
| `testimonial-2.jpg` | Social proof card 2 | Middle portrait |
| `testimonial-3.jpg` | Social proof card 3 | Right portrait |

## Heading font
The design's headings are a rounded, Title-Case display font. If that is **not**
the existing `public/fonts/Bueno.woff2`, please also drop the correct font file in
`public/fonts/` and tell me its name so headings match the design exactly.
